如果你的目标是使用 $\pi_{0.5}$ 进行**基于关节空间（Joint Space）的全身控制**（例如移动机械臂、人形机器人或带升降柱的机械臂），你的数据不需要复杂的树状结构，而是需要将所有部件的控制指令和状态**拼接成扁平化的一维向量（1D Vectors）**。

在 $\pi_{0.5}$ 和 LeRobot 的规范下，你的数据应该长这样：

### 1. 动作向量 (`action`) 的构成
在全身控制中，模型输出的 `action` 是一个一维数组，按**固定顺序**包含全身各部件的**目标关节位置/控制指令**。

假设你使用的是一个“7轴机械臂 + 1个夹爪 + 移动底盘 + 升降躯干”的系统，你的 `action` 向量（维度为 12）应该包含：
1. **机械臂关节 (Arm Joints, 7维)**：7个关节的目标角度（Target Joint Positions）。
2. **夹爪 (Gripper, 1维)**：夹爪的目标开合度（0到1的标量，或具体的电机位置）。
3. **移动底盘 (Mobile Base, 3维)**：
   * *关键注意*：$\pi_{0.5}$ 官方对移动底盘的默认处理是**速度控制**（$v_x, v_y, \omega$）。
   * *如果你坚持基于 Joint/位置*：你需要将底盘的速度积分成**全局或局部坐标位置**（$x, y, \theta$），将其视为3个“虚拟关节”；或者如果你使用的是特殊全向底盘，提供**轮子的目标关节角度**。
4. **躯干/升降机构 (Torso/Lift, 1维)**：躯干的目标升降高度或关节角度（如果是固定基座则不需要此项）。

### 2. 状态向量 (`observation.state`) 的构成
模型需要知道机器人当前的本体感受（Proprioception）才能做出决策。你的 `observation.state` 必须与 `action` 的维度**严格对应**，包含**当前的**状态反馈：
* **当前机械臂关节角度** (7维)
* **当前夹爪状态** (1维)
* **当前底盘状态** (3维，需与 action 中的底盘定义一致，如当前位置或当前速度)
* **当前躯干状态** (1维)

### 3. LeRobot 数据集的具体格式
$\pi_{0.5}$ 依赖 HuggingFace 的 **LeRobot** 数据集格式。你需要将收集到的数据转换为 **Parquet** 文件，每一行代表一个时间步（Time step），必须包含以下核心字段：

* **`observation.images`** (Dict): 包含多个相机的 RGB 图像。例如 `{"front": image_tensor, "wrist": image_tensor}`。图像需裁剪/缩放到模型要求的分辨率（通常为 224x224 或 256x256）。
* **`observation.state`** (Tensor): 形状为 `(D_state,)` 的一维浮点数数组（如上述的 12 维当前状态）。
* **`action`** (Tensor): 形状为 `(D_action,)` 的一维浮点数数组（如上述的 12 维目标关节指令）。
* **`timestamp`** (Float): 当前帧的绝对时间戳。
* **`episode_index`** (Int): 当前轨迹（Episode/Demonstration）的索引。
* **`frame_index`** (Int): 当前帧在该 episode 中的帧号（从 0 开始）。
* **`index`** (Int): 整个数据集的全局唯一索引。

### 4. 针对“基于 Joint 的全身控制”的避坑指南

1. **底盘控制模式的统一**：
   如果你收集数据时，底盘底层接收的是速度（Velocity），但你强行在数据里写成位置（Position/Joint），会导致模型学到错误的物理映射。**建议**：如果底层是速度控制，数据里的 `action` 和 `state` 就统一用速度（$v_x, v_y, \omega$）；如果底层是位置控制，就统一用位置（$x, y, \theta$）。$\pi_{0.5}$ 足够聪明，只要你的数据物理逻辑自洽，它都能学会。
2. **单位与归一化**：
   所有关节角度必须统一为**弧度 (Radians)**。在 LeRobot 的微调配置中，你需要为每个维度提供物理最小值和最大值（`min` 和 `max`），LeRobot 的预处理管道会自动将其归一化到 $[-1, 1]$ 供 $\pi_{0.5}$ 的 Flow Matching 网络使用。
3. **控制频率 (FPS)**：
   $\pi_{0.5}$ 预训练时使用了 50Hz 的数据。如果你收集的是 10Hz 或 30Hz，在 LeRobot 配置中必须正确设置 `fps`。模型内部会通过 Action Chunking（动作分块预测）来适配低频控制，但**强烈建议尽量保持 30Hz 以上的高频采集**，这对全身控制（尤其是底盘和机械臂的协同）至关重要。
4. **动作延迟补偿**：
   全身系统（特别是带底盘的系统）通常有较大的执行延迟。在收集数据时，确保 `action` 和 `observation` 在时间戳上是严格对齐的。如果存在明显延迟，需要在 LeRobot 配置中开启延迟补偿，或者在记录数据时让 `action` 提前几个时间步。

### 总结
你的数据核心逻辑就是：**`Observation State` = [当前全身关节/状态] $\rightarrow$ `Policy` $\rightarrow$ `Action` = [目标全身关节/状态]**。
只要保证 `action` 和 `observation.state` 是**维度固定、顺序一致、单位统一的一维向量**，并转换为 LeRobot 的 Parquet 格式，就可以直接用于 $\pi_{0.5}$ 的微调。